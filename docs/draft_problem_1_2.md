# 问题一、二论文初稿

> 更新日期：2026-09-10
> 状态：初稿；方法、公式和构造性验证结果均已与 `src/localization_geometry.py`、`output/results/problem_1_2_validation.json` 对照。尚无模拟器观测序列，本文不将构造案例数值表述为正式测试结果。

## 3 问题一：含方位误差的定位区域、直径及其覆盖性

### 3.1 问题分析

单个检测点给出的示向度并非确定射线。实测方位与真实方位允许相差不超过 $1^\circ$，故干扰源只能落在以检测点为顶点、张角为 $2^\circ$ 的闭角域内。多点交会由此转化为若干闭角域与题设目标圆域的交集问题。若以中心线交点代替误差角域，定位不确定性会被缩小；若以半径 $1800\ \mathrm{m}$ 圆域的外接正方形代替圆域，圆外点又会进入可行集，直径及其覆盖性随之改变。

本问的计算对象依次为定位集合、集合直径和直径圆的覆盖性。圆域边界参与相交时，定位集合的边界含有圆弧，不能沿用“只检查多边形顶点”的处理。

### 3.2 符号与假设

表 1 给出本问主要符号。

| 符号 | 含义 | 单位 |
|---|---|---|
| $S_i=(x_i,y_i)$ | 第 $i$ 个检测点坐标，$i=1,\ldots,n$ | m |
| $\theta_i$ | 在 $S_i$ 处得到的示向度；正东为 $0^\circ$，逆时针为正 | 度 |
| $\varepsilon$ | 示向度最大绝对误差，取 $1^\circ$ | 度 |
| $u(\alpha)$ | 方位角 $\alpha$ 对应的单位方向向量 | 无量纲 |
| $\Omega$ | 题设目标区域，即圆心为原点、半径为 $1800\ \mathrm{m}$ 的闭圆盘 | m$^2$ |
| $C_i$ | 第 $i$ 个示向度对应的闭角域 | m$^2$ |
| $P$ | 所有约束共同确定的定位区域 | m$^2$ |
| $D(P)$ | 定位区域 $P$ 的直径 | m |

作如下假设。

1. 对同一干扰源的每次示向度均满足题设确定性误差界 $[-1^\circ,1^\circ]$。该假设直接来自题意；若误差服从概率分布而非确定界，本模型输出的是可行域而非置信域。
2. 各检测点坐标和示向度读数采用双精度数值表示。对接近共线、相切等退化情形，引入数值容差并单独报告点、线段或空集状态。
3. 干扰源位于题设目标圆域 $\Omega$ 内。该约束为硬约束，不以外接方形近似；否则可能将距离原点大于 $1800\ \mathrm{m}$ 的点错误纳入定位区域。

### 3.3 定位区域模型

令二维叉积为

$$
\operatorname{cross}(a,b)=a_xb_y-a_yb_x,
\qquad
u(\alpha)=(\cos\alpha,\sin\alpha).
$$

其中角度在计算时转化为弧度，并按模 $360^\circ$ 处理。第 $i$ 次观测的下、上边界方位分别为 $\theta_i-\varepsilon$ 与 $\theta_i+\varepsilon$。于是，源点 $X$ 满足该次测向的充要条件可写为

$$
C_i=\left\{X:
\operatorname{cross}\big(u(\theta_i-\varepsilon),X-S_i\big)\ge 0,\\
\operatorname{cross}\big(X-S_i,u(\theta_i+\varepsilon)\big)\ge 0
\right\}. \tag{1}
$$

式（1）将误差扇形表示为两个闭半平面的交。目标圆域为

$$
\Omega=\left\{X:\|X\|_2\le 1800\right\}. \tag{2}
$$

故最终定位区域为

$$
P=\Omega\cap\bigcap_{i=1}^{n}C_i. \tag{3}
$$

半平面与圆盘均为闭凸集，故 $P$ 为紧凸集，式（4）的最大值存在。按边界形态，程序区分空集、点、线段、凸多边形和含圆弧的圆域裁剪区域。无界状态只用于显式移除式（2）的程序诊断，不属于题设下的正式输出。

### 3.4 定位区域直径的计算

定位区域直径定义为

$$
D(P)=\max_{A,B\in P}\|A-B\|_2. \tag{4}
$$

若圆域边界没有参与交集，$P$ 为凸多边形，式（4）的最大值在顶点对之间取得。此时首先枚举所有半平面边界线的两两交点，保留满足全部半平面约束且位于 $\Omega$ 内的点，再按逆时针方向形成凸包；最后枚举顶点对得到 $D(P)$ 及所有达到该值的端点对。

圆域边界参与相交时，$P$ 的边界还含有可行圆弧。此时补充计算每条半平面边界与 $\partial\Omega$ 的交点，并将可行圆弧上的对径候选纳入候选集；对候选对逐一计算距离，取最大值作为式（4）的数值解。这样，圆域约束以其原始边界进入计算，而非以外接方形近似。

算法流程如下：

1. 将每条示向度转化为式（1）中的两条半平面边界；
2. 枚举边界直线两两交点，并检验其是否满足所有半平面与圆域约束；
3. 求每条边界直线与 $\partial\Omega$ 的交点，保留可行交点；
4. 对含圆弧的情形补充可行圆弧的对径候选；
5. 对全部候选端点对计算欧氏距离，输出最大值、全部最大端点对及区域类型。

候选点数随观测次数多项式增长。针对题设有限的检测记录，枚举法的中间候选及判定条件均可复核；若观测数增加，可仅对多边形部分改用旋转卡壳法。

### 3.5 直径圆覆盖性判定

设 $(A,B)$ 为任一达到直径的端点对，则直径圆的圆心和半径分别为

$$
O=\frac{A+B}{2},\qquad r=\frac{D(P)}{2}. \tag{5}
$$

需要判定

$$
\max_{X\in P}\|X-O\|_2\le r. \tag{6}
$$

对于纯多边形区域，因圆盘为凸集，只需检验所有顶点是否满足式（6）。对于含圆弧区域，除线段端点外，还需检验每段可行圆弧上距 $O$ 最远的点；若该点不在该圆弧内，则最远值在圆弧端点处取得。本文对每个达到直径的端点对均执行上述检验，输出全部直径端点对，并以“是否存在可覆盖区域的直径圆”作为汇总结论；若用于最终展示，则应同时列出每一端点对的覆盖结果，避免在直径不唯一时任意挑选一对。

凸性不足以推出直径圆覆盖性。边长为 $D$ 的等边三角形外接圆半径为 $D/\sqrt{3}>D/2$，以任一边为直径的圆均不能覆盖第三个顶点。因此，覆盖性须按式（6）对每一条直径端点对单独判断。

### 3.6 构造验证与结果分析

由于当前尚未产生正式模拟器观测序列，以下结果仅用于验证模型和程序实现，不作为题设实例的定位答案。

表 2 的数值均来自冻结文件 `output/results/problem_1_2_validation.json`。正交交会例验证点退化；跨零度例检验 $359^\circ$ 邻域的连续处理；等边三角形则构成“直径圆必覆盖”的反例。圆弧边界的线圆交点和圆弧最远点处理由单元测试覆盖，未将其未冻结的试算数值写入正文。

| 构造情形 | 可复核输出 | 用途 |
|---|---:|---|
| $(0,0)$ 处 $0^\circ$ 与 $(1,0)$ 处 $90^\circ$ 的精确交会 | 唯一交点 $(1,0)$，$D(P)=0\ \mathrm{m}$ | 检验点退化分类 |
| $359^\circ$、$90^\circ$，误差界 $2^\circ$ | 四边形，$D(P)=0.098666\ \mathrm{m}$，直径圆不覆盖 | 检验跨零度与覆盖判定 |
| 边长 $2\ \mathrm{m}$ 的等边三角形 | $D(P)=2\ \mathrm{m}$，三条直径圆均不覆盖 | 反证“凸集必被直径圆覆盖” |

程序还保留空集、线段和显式关闭圆域时的无界诊断，避免在观测矛盾或录入异常时给出没有几何意义的有限直径。

## 4 问题二：基于最坏交会角的第二检测点选择

### 4.1 问题分析

在检测点 $S_1$ 对全向干扰源得到 `direction` 后，第二检测点既要尽量保持接收条件，又要避免两条方位线近似平行；后一情形会放大方位误差造成的定位偏差。沿首次示向度继续移动是自然的对照策略，但其交会角趋近于 $0^\circ$ 或 $180^\circ$，几何条件最差。

接口给出的有效接收半径范围为 $1000\text{--}1500\ \mathrm{m}$。以下用上界 $1500\ \mathrm{m}$ 描述第一次成功测向后的可能源区域，用下界 $1000\ \mathrm{m}$ 约束第二次的保证接收。二者分别对应“不遗漏仍可能存在的源位置”和“对任一可能位置均可接收”的口径。

### 4.2 首次测向后的不确定区域

设第一次在 $S_1$ 测得示向度 $\theta$，则潜在源位置集合为

$$
U_1=\Omega\cap C(S_1,\theta,1^\circ)\cap B(S_1,1500). \tag{7}
$$

其中 $C(S_1,\theta,1^\circ)$ 表示以 $S_1$ 为顶点、中心方向为 $\theta$ 的闭角域，$B(S_1,1500)$ 表示半径 $1500\ \mathrm{m}$ 的闭圆盘。式（7）同时利用了目标区域、方位误差和第一次已成功接收方向这一事实。

为进行可复现数值搜索，将 $U_1$ 以步长 $h$ 离散为网格点集 $\widehat U_1(h)$；第二检测点也在 $\Omega$ 内离散为候选集 $\widehat\Omega(h)$。网格只是一种数值近似，结论需通过步长加密检验，而不能把单一网格结果视为连续空间的精确最优解。

### 4.3 交会角质量指标与保证型选点模型

对真实源位置 $g\in U_1$ 和候选第二检测点 $s$，定义两条方位线的交会角为

$$
\phi(s,g)=\angle(g-S_1,g-s). \tag{8}
$$

交会角接近 $0^\circ$ 或 $180^\circ$ 时，交会定位病态；接近 $90^\circ$ 时，几何条件最佳。故取无量纲质量指标

$$
q(s,g)=|\sin\phi(s,g)|,qquad
Q(s)=\min_{g\in U_1}q(s,g), \tag{9}
$$

以 $Q(s)$ 为主排序指标，候选点不再只针对 $U_1$ 中某个假定源位置改善角度，而是比较其在整个可能源集合上的最小交会角质量。

为说明选点方向，先考察误差角域中心线上的源点。取首次示向度为局部 $u$ 轴、其法向为 $v$ 轴，设

$$
s-S_1=xu+yv,qquad g-S_1=ru.
$$

若希望交会角位于 $90^\circ\pm\gamma$ 内，可得

$$
|r-x|\le |y|\tan\gamma. \tag{10}
$$

式（10）给出中心线情形下的候选点位置：满足角度阈值的点位于首次示向度两侧的近垂直带，而不是该方向的延长线上。完整模型仍以式（9）在整个 $U_1$ 上取最小值，故没有把 $1^\circ$ 误差角域退化为一条射线。

为兼顾保证接收，定义严格候选核

$$
\mathcal K_\gamma=
\left\{s\in\Omega:
\max_{g\in U_1}\|s-g\|_2\le1000,\quad Q(s)\ge\cos\gamma\right\}. \tag{11}
$$

式（11）的距离约束以 $1000\ \mathrm{m}$ 保证第二点能够接收 $U_1$ 内任一可能源位置，角度约束限制最坏交会角。若 $\mathcal K_\gamma$ 非空，按 $Q(s)$ 降序、再按最大源距升序选择；若为空，则不存在当前口径下的保证型方案，只能输出同一排序规则下的机会性候选，并在后续获得 `direction` 或 `no_signal` 后更新不确定区域。

### 4.4 求解流程

对于给定首次示向度，第二检测点的求解步骤为：

1. 由式（7）构造并离散化首次不确定区域 $\widehat U_1(h)$；
2. 在目标圆域中生成候选检测点集 $\widehat\Omega(h)$；
3. 对每个 $s\in\widehat\Omega(h)$，计算 $Q_h(s)=\min_{g\in\widehat U_1(h)}|\sin\phi(s,g)|$ 与 $M_h(s)=\max_{g\in\widehat U_1(h)}\|s-g\|_2$；
4. 用 $M_h(s)\le1000$ 和 $Q_h(s)\ge\cos\gamma$ 筛选严格核；
5. 输出严格核的最优候选；严格核为空时输出软排序前列候选，并标记其不具备接收保证；
6. 依次减小网格步长，检查严格核的形状、候选区域和决策性质是否保持稳定。

第二点获得新示向度后，可与问题一的交集模型共同收缩可行域；若得到 `no_signal`，则可从当前候选源区域排除以第二点为圆心、半径 $1000\ \mathrm{m}$ 的保证接收范围。

### 4.5 构造验证与网格加密分析

以下验证同样基于构造首次观测，并不代表正式模拟器案例。令 $S_1=(0,0)$，首次示向度为 $359^\circ$，误差界为 $1^\circ$。以 $100\ \mathrm{m}$ 网格离散式（7），得到 15 个候选源点，位于从 $(100,0)$ 至 $(1500,0)$ 的近东向带状区域。

取两个可能源位置 $(100,0)$、$(200,0)$，比较共线前方点 $(300,0)$ 与侧向点 $(0,100)$。两点的最大源距均不超过 $1000\ \mathrm{m}$；侧向点的最坏交会角质量为 $0.447214$，共线点为 $0$。在这一对照下，保证接收不变，而侧向布点避免了共线导致的角度退化。

随后用 $200\ \mathrm{m}$、$100\ \mathrm{m}$ 与 $50\ \mathrm{m}$ 三种步长进行加密。纯几何软排序的最优点在三种网格下均为 $(800,-1600)$，最坏交会角质量分别为 $0.936329$、$0.916157$ 和 $0.905459$，显示其角度性质在网格加密下稳定；但对应最大可能源距为 $1708.801\ \mathrm{m}$、$1746.425\ \mathrm{m}$ 和 $1767.060\ \mathrm{m}$，均超过 $1000\ \mathrm{m}$，故该点不能被用于“保证第二次接收”的策略。

取 $\gamma=60^\circ$ 后，三种步长下严格核分别含 10、14、36 个网格候选。核内评分最高点依次为 $(800,-800)$、$(800,-700)$、$(750,-650)$，最大可能源距依次为 $1000.000\ \mathrm{m}$、$989.949\ \mathrm{m}$、$992.472\ \mathrm{m}$。尽管网格细化改变了代表点坐标，三组结果都落在首次方向一侧并满足保证接收约束。实际策略因而先在严格核内选点；只有严格核为空时，才采用软排序候选并等待下一次检测更新区域。

### 4.6 模型评价与适用边界

该模型把定位几何、接收约束与交会角放在同一可行性判定中，并将“角度较优”与“保证接收”分为两个层次，避免只追求交会角而选到远距离点。误差界、接收半径和网格步长均以显式参数进入模型，后续可按观测条件重算。

其适用边界也较明确。$\widehat U_1(h)$ 是 $U_1$ 的有限近似，严格核边界会随步长改变；正式使用前仍需加密网格或采用连续几何优化。以最小有效接收半径构造的保证约束较为保守；若任务允许控制失联风险，可另行以接收概率或效用替换硬约束。现有数值均为构造案例，尚未经正式模拟器观测序列检验，不能解释为真实干扰源位置或最终测试得分。
