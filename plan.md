# 2026 CUMCM B 题执行计划

> 更新日期：2026-09-10  
> 状态：问题 1–2 准备完成，等待用户批准开始正式求解。

## Why

题目要求把带误差的几何定位、检测点选择、在线搜索清除和有限次数的正式模拟测试串成一条证据链。先固定问题接口、测试门禁与结果溯源规则，避免后续算法与模拟器行为不一致，或误耗正式测试机会。

## What Changes

- 已完成：题面、附件、模拟器说明和项目经验手册的核对；问题 1–4 依赖关系及测试门禁的记录。
- 计划中：依次完成问题 1 的几何算法、问题 2 的选点策略、问题 3 的全向源策略和问题 4 的定向源策略；每步均须保留基线与验证证据。
- BREAKING：无。不得将演练/正式测试的结果视为可重复的离线数据；正式测试机会不可回滚。

## Impact

- 问题 1 为问题 2–4 提供定位区域、误差界和可验证的几何模块。
- 问题 3、4 的程序必须遵守同一接口、日志、串行请求与幂等规则。
- 任何算法、参数或模拟器代码调整都会使后续实验结论、表格和论文数字进入“待重算”状态。

## SHALL Requirements and Acceptance

1. WHEN 用户未明确批准问题 1–2 求解，THEN 不写、不运行问题 1–2 正式求解程序，也不输出问题 1–2 数值结论。
2. WHEN 进入每个问题，THEN 先完成模型合同：输入、输出、目标、约束、假设、候选方法、基线、验证与产物。
3. WHEN 使用随机或离线模拟，THEN 固定种子并记录输入、参数、命令、日志与结构化结果。
4. WHEN 启动问题 3/4 演练，THEN 先取得用户授权并确认网络、登录、时钟、端口和日志记录均就绪。
5. WHEN 启动问题 3 正式测试，THEN 先完成演练证据、测试清单并取得用户明确授权；每次启动或中止均按消耗一次机会处理。
6. WHEN 启动问题 4 正式测试，THEN 必须得到用户当次明确批准；没有该批准时，任何 UI、脚本、HTTP 调用或自动化均不得触发，且每次启动或中止均按消耗一次机会处理。
7. WHEN 写入论文数字、表格或图，THEN 数字必须来自 `output/results/` 的冻结结构化结果，并能追溯到脚本、输入和日志。

## 比赛流程

1. G1：读题、附件、规则与问题依赖图。
2. 问题 1：建立误差角域交集与区域直径算法，先做几何小例和退化情形验证。
3. 问题 2：基于问题 1 的误差传播，比较候选第二检测点策略。
4. 问题 3：先离线构造可控案例与接口桩验证，再在用户授权后进行演练；演练策略冻结后，才考虑三次正式测试。
5. 问题 4：扩展定向覆盖的不确定性表示，先离线和演练验证；**仅用户当次授权后**才可开始正式测试。
6. G4–G6：冻结结果、完成实现/数值/合理性/稳健性验证，按结果写作并审计支撑材料、日志和当届格式。

## Removed and Migration

- 无废弃功能或数据迁移。
- 若问题 1 的区域定义、误差口径或几何算法改变，问题 2–4 的依赖结果须标记失效并重新验证。
