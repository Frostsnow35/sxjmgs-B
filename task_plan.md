# 任务计划：distill 数模经验归纳与沉淀

## 目标

阅读 `distill` 中的两份经验文档和参考论文集合，提炼可复用的论文写作方法、比赛研究推进方法与质量控制原则，并形成项目级知识沉淀，供后续数学建模任务复用。

## 阶段

- [complete] 1. 盘点资料、确认文件类型与现有沉淀
- [complete] 2. 阅读两份经验文档并抽取主张
- [complete] 3. 阅读/解析参考论文并建立证据矩阵
- [complete] 4. 综合归纳：写作方法、研究方法、风险与适用边界
- [complete] 5. 写入项目经验文档/技能，并记录变更范围
- [complete] 6. 复核沉淀的可检索性、可执行性与来源可追溯性

## 关键约束

- 不把未核实的论文判断写成事实；无法确认的内容标注为“推测/TODO”。
- 论文中的数值、模型与结论只用于方法经验提炼，不虚构为本项目结果。
- 保留原有文件，不覆盖用户资料；新增沉淀要明确来源和日期。

## 当前状态

- 当前阶段：已完成第一轮沉淀
- 当前日期：2026-09-10
- 已发现：两份 Markdown 文档、参考论文集合目录、两个辅助研究目录。

## 遇到的错误

暂无阻塞错误。验证记录：必需文件均存在；参考论文文本副本为 12 个，页码标记 594 个；经验手册所需章节和 `AGENTS.md` 入口均通过检查。当前工作区不是可用 Git 工作树，未执行 `git diff --check`。

---

# 任务计划：2026 CUMCM B 题 无线电干扰源的快速自动定位与清除

## 目标

以 Manual checkpoint 模式完成 B 题四个问题的可追溯建模、算法、验证与论文支撑。用户已于 2026-09-10 明确授权开始问题一、二的离线正式求解，并于本轮授权问题三方案审查、修改与模拟器演练；**本轮禁止任何正式测试。**

## 阶段

- [complete] 1. G1 题目、附件、模拟器文档与已有经验手册核对
- [complete] 2. 记录模拟器测试门禁、竞赛流程与证据链要求
- [complete] 3. 问题 1–2：完成开题所需文档核验、模型合同草案、依赖关系与基线验证设计；等待用户批准后才进入正式求解
- [in_progress] 4. 问题 1–2：已完成几何核心、鲁棒候选核与构造案例验证；待完成网格加密和论文证据链后冻结
- [in_progress] 5. 问题 3：已完成队友 17 点基线演练，并于 2026-09-11 归档一次操作者确认的模拟演练（Runner 摘要为 12 discovered、12 cleared、0 unresolved，虚拟时间 5989.83057 s，`reported_total_sources` 恒为 null；独立 UI 证据记录用户人工提供的总数 12、全向 12、定向 0）。两类证据一致支持本次模拟演练清除全部 12 个全向源；两次均非正式测试，后续演练须确认接口就绪，正式测试须另行明确授权
- [pending] 6. 问题 4：定向源策略；先离线验证，再经用户批准启动演练；正式测试受专门门禁
- [pending] 7. 结果冻结、验证、论文证据链与提交审计

## 当前门禁与不可违反规则

1. **问题 1 求解门禁：** 未收到用户明确“批准开始问题一求解”前，只能整理题意、流程、模型合同草案和测试设计；不得输出或运行正式求解代码、数值结果或结论。
2. **问题 3 演练授权与正式测试门禁：** 问题 3 新的演练入口仅可在操作者手动确认 UI 为“问题三—模拟演练”后使用；界面或接口任何“正式测试”入口均不可点击或调用。演练日志与结果须和正式测试材料分开保存，JSONL 动作日志只保留本地。
3. **问题 4 正式测试门禁：** **未得到用户当次明确批准，禁止开始问题 4 的正式测试。** 不得通过 UI、HTTP 接口、脚本、自动化或任何间接方式触发。每次开始和手工中止都会消耗问题 4 正式测试的 1 次机会，共仅 3 次。
4. **问题 3 正式测试门禁：** 同样只有 3 次机会；开始或手工中止均占用机会。进入前须先完成演练证据、测试清单并取得用户明确授权。
5. 演练测试不限次数，但也必须先经用户授权；全程联网、登录及服务器时间误差不超过 60 秒是问题 3/4 测试前提。新测试启动截止为北京时间 2026-09-13 17:30，建议 15:30 前完成正式测试（以当届实际界面/通知复核）。
6. 每个新接口动作必须使用新 `request_id`，仅在网络异常重试同一动作时复用原请求与原 ID；动作严格串行，同时校验 HTTP 状态和 `accepted`。

## 问题 1 开始前准备清单

- 输入口径：检测点坐标、对应示向度；每个示向度相对真实方位的误差范围为 $[-1^\circ,1^\circ]$。
- 输出口径：误差扇形（射线夹角）交集形成的定位区域、其最大两点距离，以及“以该直径作圆能否覆盖区域”的严格结论。
- 候选基线：把每个检测约束表达为两条边界射线及其内侧半平面，先用小规模手算/构造案例核验区域构造。
- 候选主路线：在定位区域已证明为有界凸多边形的条件下，枚举边界交点形成顶点集，再以凸包直径算法求最大距离；需对空集、退化区域、无界可行域和角度跨 $0^\circ$ 的情形单列处理。
- 必需验证：解析小例、顶点可行性复核、与暴力顶点对距离对照，以及覆盖结论的几何证明或反例。

## 已知风险

| 风险 | 影响 | 规避动作 |
|---|---|---|
| 将带 $\pm1^\circ$ 误差的示向度当作精确射线 | 定位区域与直径被系统性低估 | 始终使用闭角域/半平面交，独立处理角度环绕 |
| 定位区域为空、退化或无界 | “多边形直径”算法可能没有定义或失效 | 在正式计算前做可行性、有界性和维数判定并报告条件 |
| 未经授权触发模拟器正式测试 | 不可恢复地消耗有限机会 | 执行层加入显式授权检查；当前阶段完全不运行模拟器 |
| DOCX 渲染工具缺少 bundled LibreOffice | 不能完成 DOCX 页面级渲染 | 保留结构化文本核验与题面 PDF 全页版面核验；如需交付/编辑 DOCX，先解决该运行时依赖 |
| 问题二的离散候选集 | 网格过粗会遗漏严格候选核或改变排序 | 记录网格步长，做步长减半的收敛核验；把有限网格结论标为近似 |
| pytest 缓存目录权限 | 不限定测试路径时会误收集不可读临时目录 | 使用 `python -m pytest -p no:cacheprovider tests -q`；不读取或删除无权限临时目录 |

## 当前状态

- 当前阶段：问题一、二的离线模型、构造验证和论文初稿已完成；问题三已完成一次新的模拟演练证据归档，仍处于正式测试前的审查与验证阶段。
- 当前日期：2026-09-11。
- 已核对文件：`issue/B/B题.pdf`（4 页）、`issue/B/附件/1.docx`、`issue/B/附件/2.docx`，以及 `emulator/emulator docs(essential)/` 下对应两份文档。

## 问题三 2026-09-11 演练证据与后续

- **已完成：** 仅在操作者确认模拟演练 UI 后，归档 Runner 自动摘要 `output/results/problem_3/20260911_rehearsal_attempt2_summary.json`，其中记录 12 discovered、12 cleared、0 unresolved 和 5989.83057 虚拟秒，且 `reported_total_sources` 保持 `null`。独立 UI 证据 `output/results/problem_3/20260911_rehearsal_attempt2_ui_evidence.json` 记录用户人工提供的总数 12、全向 12、定向 0；两类证据一致支持本次模拟演练清除全部 12 个全向源，仍非正式测试。
- **影响范围：** 本次只更新问题三演练证据、忽略规则和协作文档；不修改定位/清除算法、队友原脚本、DOCX 或正式测试状态。
- **需要验证：** UI 人工读数须连同其独立证据和来源语境保留，不能写入或冒充 Runner 自动字段；7 点覆盖、误差角域定位和接口 `accepted`/超时重试约束仍只在其数学与协议前提下成立。任何正式测试均需用户后续另行授权。

## 遇到的错误

| 错误 | 尝试次数 | 处理 |
|---|---:|---|
| DOCX 专用渲染器找不到 bundled LibreOffice，随后临时目录清理受限 | 1 | 不重复尝试；改用 DOCX 结构化文本核对及题面 PDF 全页渲染。此问题不影响题面/协议规则的文本读取，但后续编辑或交付 DOCX 前需修复。 |
| pytest 缓存创建的临时目录不可读 | 1 | 已确认只影响未限定根目录的全局收集；改用 `-p no:cacheprovider tests`，不删除无权限目录。 |
| 读取队友问题三 DOCX 时 Windows 控制台默认 GBK 无法输出数学减号 | 1 | 不重复原命令；改用 `PYTHONIOENCODING=utf-8` 后提取文本。 |
| CUA 无法枚举已置顶的模拟器窗口，并持续报 Codex 鉴权方式不支持 | 3 | 重置会话后仍失败，不再重试。改为由用户仅手动启动“问题三—演练测试”使本机端口 2026 开放，再用本地 HTTP 接口进行演练；正式测试入口始终不触碰。 |
