# 协作说明

## 开始工作

先阅读 `AGENTS.md`、`plan.md`、`task_plan.md` 和 `state/decision_log.json`。每个问题开始前，先写清输入、输出、目标、约束、假设、基线、验证和产物。

## 分支与提交

从最新 `main` 创建主题分支，完成小范围变更后提交。提交前运行与变更相关的验证，并在 PR 描述中写明：变更内容、验证命令、结果文件和未解决风险。

## 数据与结果

结果必须可追溯到题面、脚本、输入和日志。随机实验固定种子；代码、参数或数据变化后，下游图表和论文数字必须重新冻结。

## 模拟器安全

演练和正式测试都需要明确授权和测试前检查。问题 4 正式测试没有用户当次明确批准时，禁止启动；不要把参赛队号、密码或 Token 写进代码、Issue 或提交记录。
