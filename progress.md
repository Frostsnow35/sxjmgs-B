# 进度日志：distill 数模经验归纳

## 2026-09-10

- 建立任务计划、发现记录和进度日志。
- 完成 `distill` 顶层目录盘点，下一步读取文件清单与元信息。
- 读取了两份核心经验文档和一份辅助检索笔记，确认其核心主线是“得分点拆解—可解释建模—有动机求解—多角度验证—工程化表达—合规审计”。
- 盘点 12 篇参考论文：2023 年 A 题 4 篇、B 题 3 篇；2024 年 A 题 3 篇、B 题 2 篇，共约 539 页。
- 用项目已有 `extract_pdf.py` 批量生成 `distill/_research/reference_text/` 文本副本；部分 PDF 对 `pypdf` 不友好，已切换到 `fitz`，没有把解析警告误判为内容缺失。
- 完成 2023/2024 A、B 题 12 篇参考论文的对照精读：覆盖摘要、问题分析、假设/符号、模型求解、结果分析、验证/灵敏度、评价和附录；并对 4 个代表性页面做视觉核验。
- 新增项目经验手册 `references/distill_cumcm_writing_and_research_playbook.md`，包含 Why/What/Impact、证据等级、逐问模型合同、算法三问、题型路线、验证与结果冻结、72 小时流程、逐篇方法地图和失败模式修正。
- 更新 `AGENTS.md` 增加经验手册入口；更新 `findings.md` 记录证据与边界。
- 完成验证：5 个核心文件和 3 份原始经验资料路径均存在；参考论文抽取文本 12 个、页码标记 594 个；手册关键章节齐全，`AGENTS.md` 入口可检索。`git` 检查未执行成功，因为当前目录不是可用 Git 工作树；未因此修改或删除任何资料。

## 2026-09-10（B 题启动准备）

- 确认用户选择 2026 CUMCM B 题，工作方式为 Manual checkpoint；本轮不启动问题 1 求解，不运行模拟器。
- 完整读取题面 PDF、两份本地附件和 `emulator` 中对应文档；题面 PDF 已逐页渲染复核，附件与 `emulator` 文档内容对应。
- 梳理问题 1–4 的依赖、接口约束、时间模型和测试机会；把问题 4 正式测试的“必须由用户当次明确授权”写为不可绕过门禁，并对问题 3 正式测试采取同等谨慎的授权流程。
- 读取项目经验手册中的模型合同、算法三问、验证分类、结果冻结与 72 小时推进规则，纳入当前计划。
- 尝试用 bundled DOCX 渲染器进行附件版面核验 1 次；运行时缺少 LibreOffice 且临时目录清理受限，已停止重复尝试，改以 DOCX 结构化文本与题面 PDF 版面核验完成规则确认。后续若要编辑/交付 DOCX，需先修复该依赖。
- 新增 `plan.md` 与 `todo.md` 作为本赛题的轻量执行入口；其内容仅覆盖流程、验收、测试门禁和待办，不含问题 1 的求解结论。
- 文件级核验中，含中文引号的 `rg -F` 参数被 PowerShell 错误拆分；未重复该命令，改用 `Select-String` 做等价精确文本核验。该问题仅影响核验命令的参数传递，不影响文件内容。
- 首次精确文本核验因 `plan.md` 中的 Markdown 加粗符号把“用户当次明确批准”拆开而未匹配；已改为连续文本并进行一次新的完整核验。
- 探测 DOCX 替代方案：本机未安装 LibreOffice、ONLYOFFICE、WPS、Pandoc 或 `docx-preview`；现有免费可靠读取路径为 OOXML/`python-docx`，但不提供真实分页。bundled Node 环境已有 `jszip`、`playwright`，若用户授权安装开源 `docx-preview`，可用其生成 HTML 后以 Playwright 截图完成页面级检查。已发现本机 Microsoft Word，但其不是免费方案，未启动或调用。
- 用户已授权安装 `docx-preview`。首次安装命令未执行，因为 bundled Node 目录不存在预期的 `npm.cmd`；正在改用官方提供的 bundled `pnpm`，并在 `tools/docx_preview_renderer/` 隔离该工具依赖。
- `docx-preview` 已从官方 npm 源安装为 0.4.0；首次浏览器冒烟验证使用了错误的 bundled Node 路径层级，尚未启动浏览器，改用工作区依赖清单中给出的 `node/bin/node.exe`。
- 已验证免费替代链路：`docx-preview 0.4.0` + 随包 `JSZip` + 本机 Edge（由 Playwright 无头驱动）。实际将 `Instructions.docx` 渲染成功并截图；中文正文、接口文本和表格均可读。该库把该 DOCX 输出为一个连续 DOM 页面，适合内容和基础版面 QA，不等同于 Word/LibreOffice 的严格原生分页复现。
- 验证中依次排除三项运行时装配问题：Playwright 未下载自带 Chromium（改用本机 Edge）、`docx-preview` 浏览器构建需先注入全局 JSZip、pnpm 将 JSZip 放入虚拟依赖目录。上述问题均已通过本地已有组件解决，未下载浏览器、未启动或调用模拟器。
- 最后一次 `pnpm list` 在受限环境下因其 store 索引 SQLite 无法打开而失败；未重复。依赖清单、锁文件、实际 JS 文件和真实渲染截图均通过文件存在性核验，且此前实际渲染已成功。
- 重新渲染两份模拟器 DOCX，并分别检查顶部、中段、末段截图；内容、表格、代码和关键接口规则可读，无关键缺页或乱码。问题 1–2 的文档核验与开题准备现已完成，仍未开始求解。

## 2026-09-10（问题一、二离线求解启动）

- 用户已明确批准开始问题一、二求解；将继续保持模拟器零调用，问题三、四正式测试门禁不变。
- 使用 `math-modeling-contest`、`cumcm-b-optimization`、`planning-with-files-zh`、`writing-plans` 与 TDD 工作流，新增问题一、二模型合同与实施计划；下一步先写几何模块的失败测试。
- 新增 `src/localization_geometry.py`：实现闭角域半平面交、无界/退化分类、凸包直径、直径圆覆盖判定、问题二的交会角评分、严格候选核和 $U_1$ 有限网格采样。
- TDD 记录：先验证模块和脚本导入失败，再完成最小实现；当前 `python -m pytest -p no:cacheprovider tests -q` 为 12 通过。脚本直接运行一度因 `scripts/` 未包含仓库根目录而无法导入 `src`，已添加受测的根目录路径装配并验证通过。
- 运行 `python scripts/run_problem_1_2.py --output output/results/problem_1_2_validation.json`，生成仅含构造离线案例的结构化证据；未调用模拟器。完成 200/100/50 m 网格加密：软排序最优点保持为 $(800,-1600)$ m 但不保证接收，严格核最优候选收敛在 $(750\text{–}800,-650\text{–}-800)$ m 的侧向区域。
- 根据队友方案审查修复问题一圆域遗漏：`locate_from_bearings` 默认加入 $B((0,0),1800)$，并为圆弧边界增加线圆交点与最远圆弧点检查；14 项回归测试通过，未调用模拟器。
- 新增 `docs/draft_problem_1_2.md`，形成问题一、二可并入正文的中文初稿。初稿已将题设圆域作为硬约束，区分多边形与含圆弧区域；问题二明确严格候选核、软排序和网格加密。所有数值均标记为构造验证案例，未表述为模拟器结果。
- 参照 `research-writing-skill`、项目论文经验手册与 `bzd-paper-aigc-auditor` 的检查项，完成问题一、二初稿的第二轮学术表达修订：加强每段的对象、约束和证据对应关系，减少模板化连接词、无据修饰与三段式排比；以表格承载冻结的构造验证结果。未运行模拟器，未改变模型公式、代码或冻结结果文件。

## 2026-09-10（问题三方案审查与演练）

- 用户授权同步队友的问题三方案、整理本地文件，并可进行模拟器演练；本轮明确禁止正式测试。
- 已核对远端 `main`：队友提交 `b818d92` 已在本地历史中，无待拉取提交；该提交含 `q3-solution/code/` 下的客户端、扫描和测试脚本，以及问题三说明 DOCX。
- 初步盘点发现队友提交包含已跟踪的 Python `__pycache__/*.pyc`，后续整理时应从版本库移除并以 `.gitignore` 约束；在完成审计和可复现迁移前不删除原方案文件。
- 已完成题面、接口规范、队友 DOCX 与 4 个 Python 脚本的静态交叉审计，形成 `docs/problem_3_audit.md`。结论是现有方案不能证明“确保所有干扰源被清除”；已给出 7 点首次扫描覆盖证明、一次示向后的保证型复测构造、闭角域定位与 20 m 覆盖兜底的重构建议。
- 由于 CUA 运行时未暴露已启动的模拟器窗口且报告鉴权错误，未尝试通过 HTTP 端口探测或调用接口，避免在无法确认当前模式时误触正式测试。等待可确认的“问题三—演练测试”界面后再进行演练。
- 已更新 `state/decision_log.json`：记录本轮问题三演练授权、正式测试禁令、静态审计结论与当前 UI 自动化阻塞。未改动队友代码或运行其脚本。
- 用户要求先进行现有方案的模拟基线演练。窗口识别显示 `E:\CUMCM2026\emulator\Jammers-simulator\jammers-simulator.exe` 进程正在运行（PID 24288），但无主窗口标题，端口 2026 没有监听；CUA 仍因鉴权错误无法发现应用窗口。未发送任何 HTTP 请求，不能据此确认或启动演练。
- 用户确认模拟器窗口已置顶后，重置 CUA 会话并再次捕获，结果仍为 `apps=[]` 且报 `unsupported Codex auth method: apikey`。该自动化桥连续三次未能暴露本地应用窗口，按故障处理规则不再重复尝试；仍未调用模拟器接口。
- 用户手动启动问题三演练并明确反馈接口就绪后，运行队友原始 `test_run.py` 取得基线：12 个发现频道全部清除，虚拟时间 10896.8 s，340 次扫描测向和 46 次逼近测向。其汇总 CSV 已复制为规范化结果文件 `output/results/problem_3/baseline_teammate_17point_20260910.csv`；不将该脚本的“发现频道清除率”表述为全源清除率。
- 用户选择项目内 `.worktrees/` 作为问题三重构隔离位置。下一步先提交本轮演练证据和忽略规则，再从干净 `main` 建立 `refactor/problem-3-safe-rehearsal` 工作树。

## 2026-09-11（问题三模拟演练证据归档）

- **已完成：** 仅在操作者手动确认 UI 为“问题三—模拟演练”后，归档成功摘要 `output/results/problem_3/20260911_rehearsal_attempt2_summary.json`。摘要记录状态 `completed`、12 discovered、12 cleared、0 unresolved 与 5989.83057 虚拟秒；这是模拟演练，不是正式测试。
- **已完成：** 用户 UI 人工回填为总数 12、全向 12、定向 0，并写入摘要的 `reported_total_sources`；该界面读数与摘要中的发现/清除计数一致。动作明细 `logs/problem_3/20260911_rehearsal_attempt2_actions.jsonl` 保持本地忽略，不提交。
- **影响范围：** 仅整理问题三的成功演练结果、Git 忽略规则、缓存索引和协作文档；不改变数学模型、接口实现、队友原始脚本或 DOCX，也不触发任何模拟器/HTTP 动作。
- **需要验证：** 7 点覆盖、闭角域定位与协议重试规则仍须在其假设和 `accepted` 响应下核验；用户提供的 UI 回填应保留其人工来源。若需问题三正式测试，必须由用户在后续单独授权并由操作者再次确认 UI 模式。
