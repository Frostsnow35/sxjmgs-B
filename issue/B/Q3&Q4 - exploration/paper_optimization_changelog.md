# 论文优化变更记录

日期：2026-09-11

## 第三轮加速（v3，代码已生效，待正式演练验证）

1. Q4/Q3 巡测改为**条件扫描**：某频道累计 2 次 `direction` 或 1 次 `near` 后，在后续巡测点跳过，仍保持发现完备性。
2. Q4 单信号频道增加**垂直双探针**：在原测点法向两侧各 100 m 探测，用于快速获得第二方位。
3. Q4 MEC 半径 `20<R≤25 m` 时增加**原位试探清除**；失败仅 3 s。
4. 单信号扇区兜底改为**双径向轨道覆盖**，替代方形网格，缩短兜底路径。
5. 巡测频道扫描顺序改为在 1–20 线上从当前频道出发、近侧优先的最短切换顺序。
6. 本地 100 案例：Q4 平均 `589.4 s/源`、总时间 `7362.9 s`；Q3 平均 `409.0 s/源`、总时间 `5131.7 s`；清除率均 100%。

---



## 备份

- 修改前原稿备份：
  - `Q3&Q4 - exploration/backup_20260911_0918/draft_problem_3.md`
  - `Q3&Q4 - exploration/backup_20260911_0918/draft_problem_4.md`

## 代码与研究优化

1. `strategy.q4_survey_points()` 由硬编码 31 个坐标改为**三角格生成算法**：
   - 整数指标 `(i,j)` + 基向量 `u=(1000,0)`、`v=(500,500√3)`；
   - 保留所有到原点最近距离不超过 1800 m 的三角形；
   - 输出仍为 31 个顶点，`survey_design.py` 复验通过。
2. 新增 `route_study.py`，量化巡测路径与扫描顺序：
   - Q3：路径 9353.074 m = MST 下界；巡测虚拟时间 2703.615 s；
   - Q4：路径 30732.051 m，比逐行蛇形减少 3377.802 m，与 MST 下界间隙 2.44%；巡测虚拟时间 9835.410 s；
   - 每点循环扫描频道，固定 19 次切换。
3. 生成 `route_optimization_study.json` 作为论文数据来源。

## 论文初稿优化

- `context/draft_problem_3.md`
  - 新增巡测路径与循环扫描顺序的量化论述；
  - 修正初始 MEC 半径公式，区分 `1/cos(1°)`（扇形 MEC）与 `1/cos(π/180)`（180 边形外近似）；
  - 新增引理 2（扇形最小包围圆）与定理 1（MEC 收缩、至多 6 次复测）；
  - 新增定理 2（问题三有限清除保证）；
  - 方程编号顺延为 (21)–(30)。
- `context/draft_problem_4.md`
  - 6.3 节改写为算法 1：三角格巡测点生成算法；
  - 新增巡测路径与扫描顺序优化量化；
  - 引理编号改为引理 3，新增定理 3（问题四有限清除保证）；
  - 方程编号顺延为 (31)–(34)。

## 引用与来源

- 新增 `context/docs/citation_provenance.md`，记录：
  - 题设、附件、模拟器日志、客户端脱敏日志的来源；
  - 公开文献 [1] Welzl MEC、[2] Sutherland–Hodgman / SciPy 的实际使用位置；
  - 本文推导结论与公开文献的边界。
- 本次优化没有新增外部文献；引用清单与两份初稿原引用一致。

## Q4 联合可行性投影（2026-09-11，当前实现）

1. 新增 `joint_feasibility.py`：对固定候选位置解析消去未知有效半径，取
   $r_{\min}=\max\{1000,\max_i\|g-p_i\|\}$，并以正信号闭半圆弧和在范围内负测量的开反半圆弧求交；不离散朝向角。
2. 将凸包内 `no_signal` 解释为可证明的 $\|g-q\|>1000$ 安全特例；凸包外负测量保持“距离不足或定向反侧”的析取语义。
3. 联合投影不再替换 MEC 复测点，也不提前清除。它只为保守网格/扇区覆盖候选选择首个优先访问点；覆盖候选全集保持不变。
4. 固定种子 50 组 LocalEnv 配对：关闭/开启联合排序均清除 $646/646$ 个源；开启后平均少 $0.66$ 次清除动作、平均虚拟时间差 $+0.056$ s/轮。该差异不支持速度优越性主张，故论文仅将其表述为稳健性与信息利用改进。
